// import React from 'react';

export const mention = ({key, list}) => ({
    onKeyDown(e, change) {
      // var test = change.value.blocks.map(block => {
      //   console.log(block.text);
      // })
      console.log(change.value.texts);

    }
  }
)
